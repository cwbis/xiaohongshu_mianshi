## 1. 自动化健康检查

- [x] 1.1 运行 `npm run build`，确认 React 前端可构建。
- [x] 1.2 运行 `python -m unittest test_local_app_server.py`，确认后端测试通过。
- [x] 1.3 临时启动 `local_app_server.py`，验证 `/api/health` 和首页 React 应用可访问。
- [x] 1.4 检查 Git 工作区状态，确认巡检前没有未解释的异常改动。

## 2. 核心功能手动巡检

- [x] 2.1 巡检启动方式：确认 `python local_app_server.py` 和 README 中的运行方式仍正确。
- [x] 2.2 巡检采集页：确认搜索表单、刷新、本页翻页、候选详情、勾选入库入口可用。
- [x] 2.3 巡检帖子库：确认公司导航、岗位筛选、卡片网格、详情弹层、删除和载入草稿入口可用。
- [x] 2.4 巡检分析页：确认规则分析报告、典型问题和专题题库入口可用。
- [x] 2.5 巡检大模型页：确认供应商、模型、Base URL、API Key 配置表单和生成答案入口可用。

## 3. 图片加载问题排查

- [x] 3.1 检查采集结果和已入库帖子中的 `coverUrl` 数据，确认图片为空、过期或外链失败的比例。
- [x] 3.2 在浏览器或本地 smoke 环境中复现图片加载失败，确认失败发生在候选卡片、帖子库卡片还是详情页。
- [x] 3.3 检查 `LibraryNote`、`CollectPage` 和详情弹层的图片渲染逻辑，定位是否缺少 `onError` 降级。
- [x] 3.4 判断是否需要新增 `/api/image-proxy`；如前端降级足够，则记录不新增代理的原因。

## 4. 图片体验修复

- [x] 4.1 为帖子图片组件增加加载失败状态，失败后显示小红书风格占位封面。
- [x] 4.2 为采集候选卡片图片增加错误降级，避免破图图标和布局塌陷。
- [x] 4.3 为详情页视觉区域增加图片失败兜底，确保正文仍可阅读、弹层仍可滚动。
- [x] 4.4 如确认需要图片代理，则实现受限的 `/api/image-proxy` 并接入前端；否则不新增后端接口。

## 5. 文档与回归

- [x] 5.1 更新 README 或 docs，记录功能巡检命令和图片加载降级策略。
- [x] 5.2 重新运行 `npm run build` 和 `python -m unittest test_local_app_server.py`。
- [x] 5.3 重新验证 `/api/health`、首页 React 应用和帖子图片降级效果。
- [x] 5.4 提交并推送修复结果到 GitHub。
