## 1. 数据库基础层
- [x] 1.1 在本地 Python 服务中加入 SQLite 初始化、schema 版本管理和连接辅助逻辑。
- [x] 1.2 为帖子、配置 scope 和迁移元数据建立 repository 辅助方法，并保证写入具备事务性。
- [x] 1.3 明确帖子在插入和更新时基于 `id`、`noteId`、`sourceUrl` 的身份识别与去重规则。

## 2. 持久化 API 与迁移
- [x] 2.1 增加一个 bootstrap 存储接口，在一次响应中返回已保存帖子、已保存配置和存储元数据。
- [x] 2.2 增加帖子集合与分 scope 配置的读取、更新持久化接口。
- [x] 2.3 增加一次性旧版浏览器数据导入接口，用于接收浏览器侧历史数据并写入 SQLite。
- [x] 2.4 增加接口级校验和显式错误响应，覆盖非法 payload 与数据库写入失败场景。

## 3. 前端接入
- [x] 3.1 把 `store.js` 从直接访问 `localStorage` 重构为异步 API 客户端，负责 bootstrap、帖子和配置读写。
- [x] 3.2 更新 `app.js` 启动流程，使其在后端存储数据就绪后再渲染依赖状态的页面内容。
- [x] 3.3 将 UI 中长期数据的保存逻辑替换为后端持久化调用，并仅把瞬时界面状态保留在内存中。
- [x] 3.4 增加首次运行的历史浏览器数据迁移逻辑，同时保留 JSON 导入导出作为恢复手段。

## 4. 托管运行时与运维
- [x] 4.1 增加轻量桌面启动器，在无手动终端命令的情况下启动本地后端、等待 `/api/health` 并打开 UI。
- [x] 4.2 确保托管运行时可以清晰提示启动失败，并在退出时正确关闭后端服务。
- [x] 4.3 更新项目文档，说明新的默认启动方式、开发兜底启动方式、数据库文件位置和迁移行为。
- [x] 4.4 增加数据库 bootstrap、持久化 API、迁移流程以及托管启动健康检查的验证覆盖。
