## 1. 发布前审计

- [x] 1.1 使用 `rg` 检查旧前端文件名和全局对象引用，确认 React 构建与 Python 服务不依赖旧入口。
- [x] 1.2 梳理文件清理清单，标记可删除文件、需保留文件和需用户确认文件。
- [x] 1.3 检查仓库中是否存在本地数据库、Cookie、API Key、日志、缓存和生成物。

## 2. 清理旧文件与忽略规则

- [x] 2.1 删除确认不再使用的旧全局前端文件。
- [x] 2.2 删除临时日志、测试输出和不应发布的本地运行文件。
- [x] 2.3 新增或更新 `.gitignore`，排除 `node_modules/`、`dist/`、`data/*.db`、日志、环境文件和缓存目录。
- [x] 2.4 更新 README 和重构说明，移除对旧前端入口的描述，保留 React 运行方式。

## 3. 回归验证

- [x] 3.1 运行 `npm run build`，确认清理后 React 前端仍可构建。
- [x] 3.2 运行 `python -m unittest test_local_app_server.py`，确认后端测试仍通过。
- [x] 3.3 临时启动 `local_app_server.py`，验证 `/api/health` 和首页 React 应用可访问。
- [x] 3.4 检查 Git 待提交文件，确认不包含本地数据库、密钥、依赖目录或构建产物。

## 4. GitHub 发布

- [x] 4.1 检查当前目录是否已有 Git 仓库；如无则初始化 Git。
- [x] 4.2 确认 GitHub 目标仓库名、可见性和远程地址。
- [x] 4.3 创建清理与发布提交。
- [x] 4.4 推送到用户 GitHub 账号下的目标仓库。
- [x] 4.5 在最终输出中提供 GitHub 仓库地址和本地运行命令。
