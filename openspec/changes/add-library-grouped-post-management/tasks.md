## 1. 帖子库分组数据层

- [x] 1.1 在 `app.js` 中补充分组视图所需的页面状态，例如视图模式、公司组展开状态和岗位组展开状态。
- [x] 1.2 基于 `state.posts` 实现帖子库分组派生函数，按 `company` 一级分组、按 `role` 二级分组，并计算帖子数量与最近采集时间。
- [x] 1.3 为缺失 `company` 或 `role` 的帖子增加“未分类公司 / 未分类岗位”兜底标签，并保证排序结果稳定。

## 2. 帖子库界面改造

- [x] 2.1 改造 `renderLibraryPage`，增加“分组视图 / 平铺视图”切换入口和分组概览容器。
- [x] 2.2 新增公司组与岗位子组的渲染逻辑，展示组摘要、帖子数量、最近采集时间和组内帖子列表。
- [x] 2.3 保留并复用现有帖子卡片展示，使分组视图中的帖子仍支持原有元信息、原帖链接和全文展示。

## 3. 组内管理交互

- [x] 3.1 将查看全文、载入草稿、删除等现有帖子管理动作接入分组视图，并确保操作后刷新对应分组。
- [x] 3.2 为公司组和岗位组增加展开 / 收起交互，避免长列表一次性铺满页面。
- [x] 3.3 确保从分组视图切回平铺视图后，帖子列表仍按原有时间顺序正常展示。

## 4. 验证与文档

- [x] 4.1 自检帖子库在空状态、单公司、多公司、缺失字段帖子场景下的分组表现。
- [x] 4.2 验证删除、载入草稿、全文展开在平铺视图和分组视图下都能正常工作。
- [x] 4.3 如交互或入口有明显变化，补充 `README.md` 或相关说明文案，简要描述新的帖子库浏览方式。
